{% extends 'base.html.twig' %}

{% block title %}Nouveau produit{% endblock %}

{% block body %}
    <h3> <div class=" pt-4 pb-4"> Ajouter un produit</div></h3>

   {# ## {{ include('produit/_form.html.twig') }} #}


{{ form_start(form) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>

     <div class="form-group">
        {{form_row(form.titre, {"attr" : {'class': "form-control" }})}}
    </div>
    <div class="form-group">
        {{form_row(form.description, {"attr" : {'class': "form-control" }})}}
    </div>
    <div class="form-group">
        {{form_row(form.prix, {"attr" : {'class': "form-control" }})}}
    </div>
    <div class="custom-control custom-checkbox custom-control-inline">
        {{form_row(form.couleur, {"attr" : {'class': "d-flex justify-content-evenly custom-control-input" }})}}
    </div>
    <div class="custom-control custom-checkbox custom-control-inline">
            <label for="{{form.taille.vars.id}}" class="pr-3 "></label>
            {{form_row(form.taille, {"attr" : {'class': "d-flex flex-wrap justify-content-evenly custom-control-input" }})}}
        
        </div>
{{ form_end(form) }}
    
    </div>



    <a href="{{ path('app_produit_index') }}">Retour à la liste</a>
{% endblock %}
