{% extends 'base.html.twig' %}

{# {% block title %}Hello HomeController!{% endblock %} #}

{% block body %}


<div class="zoneCard d-flex flex-wrap justify-content-evenly">  
    {% for produit in produits %}


    
        <div class="card h-50 "  style="width: 12rem " >
            <div class="d-flex">
            {# {% for image in produit.images %} #}
            <img src="{{  asset('/uploads/' ~ produit.images[0].nom) }}" class="card-img-top" alt="image" width="200" height="200" >
            {# {% endfor %}  #}
            </div>

            <div class="card-body ">
                <h6 class="card-title">{{ produit.titre }}</h6>
                {# <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p> #}
            </div>

            
                <p class="list-group-item">{{ produit.prix }} €</p>
            
            <div class="card-body d-flex justify-content-between">
                <a href="{{path('app_produit_show', {id: produit.id})}}" class="card-link">Voir plus</a>
                <a href="{{path('app_panier_index', {id: produit.id})}}" class="card-link"><img src="{{asset('assets/images/sac-de-courses.png')}}" alt=""></a>
            </div>
        </div>
    

            {% else %}
                <div>
                    <p>Aucun produit</p>
                </div>

    {% endfor %}
</div>       
    

{% endblock %}
