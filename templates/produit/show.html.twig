{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h2>{{ produit.titre }}</h2>
 <img src="{{  asset('/uploads/' ~ produit.images[0].nom) }}" alt="image">
    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr> #}
            <tr>
                <th>Titre</th>
                <td>{{ produit.titre }}</td>
            </tr>
            <tr>
                <th>Description </th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }} €</td>
            </tr>
            {# <tr>
                <th>Quantite</th>
                <td>{{ produit.quantite }}</td>
            </tr> #}
            <tr>
                <th>Couleur</th>
                <td>{{ produit.couleur.name }}</td>
            </tr>
            {# <tr>
                <th>Taille</th>
                <td>{{ produit.taille }}</td>
            </tr> #}
        </tbody>
    </table>

    {# <a href="{{ path('app_produit_index') }}">Retour à la liste</a> #}

    {# <a href="{{ path('app_produit_edit', {'id': produit.id}) }}">Modifier</a> #}

    <button type="submit" class="btn all btn-primary mb-5"><a href="{{path("panier_add", {id:produit.id})}}"  class="text-decoration-none text-white ">Ajouter au panier</a></button> 

    {# {{ include('produit/_delete_form.html.twig') }} #}

{% endblock %}
